# docker-compose.yml

services:
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
      target: prod  
    ports:
      - "5173:80"
    environment:
      - VITE_API_URL=http://localhost:8000

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "8000:8000"