# docker-compose.yml

services:
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
      target: prod  
    ports:
      - "5173:80"
    environment:
      - VITE_API_URL=http://localhost:8000
      # - VITE_API_URL=https://polybrawl.me 

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8000:8000"